@model gongshangchaxun.Models.LocalPasswordModel
@{
    ViewBag.Title = "管理帐户";
    Layout = "~/Views/Shared/_Layoutgl.cshtml";
}

@*<hgroup class="title">
    <h1>@ViewBag.Title.</h1>
</hgroup>
<p></p>*@

     <div style="border-bottom:solid 1px #b5cbe3; margin-top:30px;">
         <div style=" float:left; padding-left:5px;font-size:18px; ">您正在使用 <strong>@User.Identity.Name</strong>. 身份登录！</div>
         <div style="float:right;">
            @{
                if(User.Identity.Name.ToString()=="admin")
                {
                    <a href="/admin/"><img src="/content/images/fanhui.png" style="float:right; margin-top:1px; margin-bottom:2px; margin-right:50px; cursor: pointer;border-width:0px 0px 0px 0px;height:30px;"/></a>
                }
                else
                {
                     <a href="/gl/"><img src="/content/images/fanhui.png" style="float:right; margin-top:1px; margin-bottom:2px; margin-right:50px; cursor: pointer;border-width:0px 0px 0px 0px;height:30px;"/></a>
                }
                
                }
             
              
         </div>
         <div class="clr"></div>
     </div>

<p class="message-success">@ViewBag.StatusMessage</p>

@if (ViewBag.HasLocalPassword)
{
    @Html.Partial("_ChangePasswordPartial")
}
else
{ 
    @Html.Partial("_SetPasswordPartial")
}

<section id="externalLogins">
  @*  @Html.Action("RemoveExternalLogins")

    <h3>添加外部登录</h3>
    @Html.Action("ExternalLoginsList", new { ReturnUrl = ViewBag.ReturnUrl })*@
</section>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
